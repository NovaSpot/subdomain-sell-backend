"""
we use the model names as singular for eg: user.py.

about client-side hashing, for passwords, it is secure, but it could be cpu or ram -intensive.
some platform like bitwarden, protonmail etc do both client-side and server-side hashing. but 
the industry standard is to hash passwords on the server-side, and not on the client-side. 
https is another layer on top of this, which encrypts the data in transit. 



"""